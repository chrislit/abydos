# Library dependencies for the Python code.  You need to install these with
# `pip install -r requirements-dev.txt` before you can run this.

nltk
syllabipy
lzss
paq
sphinxcontrib-bibtex
sphinx_rtd_theme
